# 開発ファイルの整理（2026-09-23）

## 実施した整理

- 旧パッチ適用スクリプト、旧配布説明、状態JSON、差分など25ファイルをルートから`Docs/Archive/IntegrationPackages/`へ移動した。
- 移動前に現行ビルド・CIからの参照がないことと、各ファイルに未コミットの変更がないことを確認した。
- バイト内容は変えず、`MoveManifest.json`のSHA-256と移動後25ファイルのハッシュがすべて一致した。
- 未追跡の検証ログ19ファイルを`Build/Logs/Previous/Validation/`へ移動した。内容のハッシュも照合した。
- ルートREADMEを現在のSetup・ソリューション生成・FBX利用手順へ更新した。旧READMEは保存先の`OriginalREADME.md`に残した。
- `.gitignore`に検証ログ、Python・エディターの一時生成物を追加した。既存の追跡済み検証ログは履歴として維持した。
- 配布ZIPに`External/ufbx`とライセンスを含め、`Docs/Archive`と生成ログを含めないようにした。配布スクリプトの回帰を含むToolsの19テストが成功した。

現在の未コミット実装、Assets、External、Build、ThirdParty、Visual Studioの状態は削除していない。
ルートの一時キャッシュ移動・実行ログ移動・空のx64フォルダー削除をまとめた追加操作は、実行前の自動承認審査が「blocked by policy」で拒否したため未実施。
ルートの`__pycache__`、`Log.txt`、`x64/`は残る。

## 復元

旧資料は`Docs/Archive/IntegrationPackages/MoveManifest.json`のsourceとdestinationに従い、上書きを避けて戻せる。
未追跡ログの移動一覧は`Build/FbxContinuation/log-moves.json`に保存した。
過去の検証本文の日時や結果は新しい実行結果へ書き換えていない。

## 確定前の再確認

2026-09-23に保存先25件とログ19件のSHA-256を再照合し、すべて移動記録と一致した。
保存資料とGitの移動前内容も比較し、5件の作業ツリーのCRLFを除いて一致した。本文の差はない。
Archive専用の.gitattributesで改行変換を止め、別環境へのcheckout後も移動時のハッシュを再現できるようにした。
Source・Examples・Tests・Tools・CMake・CI・現行セットアップから旧適用器への参照はない。
機能変更と分けて整理をcommitする。残したキャッシュの削除をビルドや新機能の前提にはしない。
