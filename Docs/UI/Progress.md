# UI基盤の状態 — 2026-09-27

Windows／実DxLibでの仕上げ（W0〜W7）後の状態です。実行結果と条件は[検証記録](../Development/UiWindowsCompletion-2026-09-26.md)、以前のLinux環境での状態は[中断UI基盤の継続記録](../Development/UiResume-2026-09-26.md)を参照してください。「CPU」は代替の描画先・文字計測による試験、「実描画」は実DxLibの画素検査、「外部」は再配置したパッケージを使う外部Applicationです。

| 段階 | 実装 | CPU試験 | 実Application／実描画 | 外部利用 | 未完了・保証しないこと |
|---|---|---|---|---|---|
| U1 木・参照・寿命・購読 | 済 | 済（変異：購読の解除漏れを検出） | UISample・NativeUiSmokeで使用 | NativeUiApp | 任意スレッドからの直接操作 |
| U2 レイアウト・文字・スタイル・倍率 | 済 | 済 | 実フォントの日本語の画素、倍率1／2のViewport | 組込み／外部スタイル | OS DPI・ウィンドウのリサイズ操作 |
| U3 入力・フォーカス・キャプチャ | 済（Shift＋ホイールの横を追加） | 済（押下中の非表示・破棄・切断、Modal、左右のプレイヤー） | 固定入力でのクリック・ドラッグ | 固定入力 | 人による物理入力、OSウィンドウ外のキャプチャ |
| U4 コントロール | 済（ScrollViewの横・両方向、時間基準の慣性を追加） | 済（30／60／144Hzの一致） | Slider・Toggle・一覧・確認Popupを実DxLibで操作 | ボタン | 高度なテキスト編集 |
| U5 クリップと表示先 | 済（3D平面の透明な合成を追加） | 済（クリップ受渡し） | 全画面・幅641／639のViewport・2Dパネル・3Dの不透明／透明パネル・隠れ・実画像 | 画面・3D平面 | 2Dパネルの透明合成、交差する透明形状の厳密な合成、Backendの乗算済み合成の対応を問い合わせる窓口（テクスチャ付き四角形を描けないBackendは失敗を返す） |
| U6 データ・スタイル資源 | 済 | 済 | 明示再読込 | 外部スタイル・読込失敗後の旧版保持 | 自動監視、CSS／USS互換 |
| U7 ゲーム側の利用 | 済（確認Popup） | 軌跡の完全一致（UIなし／1／2表示、一時停止のずれ） | UISample 2D／3Dの全経路 | NativeUiApp（2D→3D） | 人による長時間の操作 |
| U8 検査・測定・配布 | 済（出所付き検査・InspectDisplay・割当削減） | 済 | — | 配布4構成（Native OFF／ON × Debug／Release） | 実字体・実GPUを含む性能測定、SDK未導入PC |

- 割当：静止1000要素／1表示の命令生成〜キュー受付は、改善前（`161f6af`）の2009回/フレームから0回/フレーム（同じPC・同じ条件）。全UIの確保ゼロではなく、スタイル再読込・ルート作り直しは変更・配置の区間で割当が残ります。
- 最終回帰（`eb0dc18`）：root CTest Debug／Release 32/32、ValidateDebug 26/26×2、配布4構成成功。
- `NativeModelDeviceSmoke`の`ProcessMessage=-1`は今回再発していませんが、原因未特定で解決済みではありません。
- Visual Studioの実F5操作、物理入力・聴感、実D3D9の個別検証、D3D／COM全資源のリーク列挙は未実施です。
